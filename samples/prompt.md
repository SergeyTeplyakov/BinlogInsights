The build is failing. The binlog file is in this directory.

Investigate the build failure and fix the code so it builds successfully.
Do not change the intent of the code — only fix what's broken.

After applying fixes, rebuild with `dotnet build /bl` and confirm the build succeeds.
